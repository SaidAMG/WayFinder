@{
    ViewData["Title"] = "Home Page";
}

<div id="modalPlaceholder"></div>

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<button type="button" class="btn btn-primary" onclick="openRegisterModal()">Sign up as Guest</button>

<script type="text/javascript">
    function openRegisterModal() {
        $.post('@Url.Action("OpenRegisterGuestModal", "Home")', function(response) {
            $('#modalPlaceholder').html(response);
            $('#registerForEventModal').modal('show'); 
        });
    }
</script>

@* Feedback na submit toevoegen (Je bent geregistreerd) *@
@*<div class="row">
    <form asp-action="RegisterGuest">
        <div class="form-group">
            <label asp-for="FullName" class="control-label">Full name</label>
            <input asp-for="FullName" class="form-control"/>
        </div>
        <div class="form-group">
            <input type="submit" value="Register as Guest" class="btn btn-primary"/>
        </div>

    </form>
</div>*@